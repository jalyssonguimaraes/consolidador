export function AllocationCharts({allocation,sectors,total}:{allocation:[string,number][];sectors:[string,number][];total:number}){const bars=(items:[string,number][],cls='')=><div className="chart-bars">{items.map(([label,value])=><div className="bar-row" key={label}><div><span>{label}</span><b>R$ {value.toLocaleString('pt-BR',{minimumFractionDigits:2})}</b></div><div className={'bar '+cls}><i style={{width:`${total?value/total*100:0}%`}}/></div></div>)}</div>;return <section className="charts-grid"><section className="surface chart-card"><h2>Distribuição por classe</h2><p className="note">Baseada no custo histórico das posições abertas.</p>{bars(allocation)}</section><section className="surface chart-card"><h2>Setores das ações</h2><p className="note">Classificação inicial por ticker.</p>{bars(sectors,'sector')}</section></section>}
